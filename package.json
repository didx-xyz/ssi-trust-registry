{
  "private": true,
  "name": "ssi-trust-registry",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspace @ssi-trust-registry/backend build",
    "validate": "yarn workspace @ssi-trust-registry/backend validate",
    "test": "yarn workspace @ssi-trust-registry/backend test",
    "start": "yarn workspace @ssi-trust-registry/backend start",
    "postinstall": "node -e \"process.env.NODE_ENV !== 'production' && require('husky').install()\""
  }
}
